# This is an open-source project: always resolve from the public registry,
# regardless of the developer's global npm configuration.
registry=https://registry.npmjs.org/
engine-strict=true
# Never run arbitrary lifecycle scripts from dependencies unless allow-listed
# in pnpm-workspace.yaml (onlyBuiltDependencies).
ignore-scripts=false
save-exact=true
