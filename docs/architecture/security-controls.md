# Security controls

| #   | Control                                                                                        | Enforced in                                                                                                                                         | Tested by                                             |
| --- | ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| C1  | Agents hold no GitHub credentials                                                              | `workers/agents/wrangler.jsonc` has no PAT bindings; `CustodesAgent.act()` only calls the broker                                                    | review checklist, `security-reviewer`                 |
| C2  | Policy allow-list per agent (repos, actions)                                                   | `packages/core/src/policy-eval.ts`, `workers/github-broker/policy/policy.json`                                                                      | `policy-eval.test.ts`, `act.test.ts`                  |
| C3  | Kill switch (agent or fleet)                                                                   | `KillSwitch` checked in `CustodesAgent.guard()` and again in broker `act()`                                                                         | `killswitch.test.ts`, `act.test.ts`, `hello.test.ts`  |
| C4  | Approvals bound to exact action, single-use, expiring                                          | `ApprovalStore` + `act()` action-hash compare; `approval-token.ts`                                                                                  | `approval-token.test.ts`, `act.test.ts`               |
| C5  | Rate limit per agent                                                                           | broker `act()` counting allowed audit rows per hour                                                                                                 | `act.test.ts` (extend)                                |
| C6  | Signed audit record for every request (allowed or denied); private keys never leave the broker | `Keyring` Durable Object (`workers/github-broker/src/keyring.ts`), `AuditStore.insert`                                                              | `attestation.test.ts`, `act.test.ts` (incl. rotation) |
| C7  | Public verification                                                                            | gateway `/verify/:id`, CLI `audit verify`                                                                                                           | `verify.test.ts`                                      |
| C8  | Untrusted content envelope + structured output                                                 | `@custodes/llm` `untrusted()`, `parseStructured()`                                                                                                  | `untrusted.test.ts`, `structured.test.ts`, evals      |
| C9  | Webhook signature verification                                                                 | `verifyGitHubWebhookSignature`                                                                                                                      | `webhook.test.ts`, `gateway.test.ts`                  |
| C10 | Inbound e-mail authentication + allow-list                                                     | `isTrustedSender`                                                                                                                                   | `auth.test.ts`                                        |
| C11 | Access JWT verified in-Worker                                                                  | `verifyAccessJwt` middleware                                                                                                                        | `gateway.test.ts`                                     |
| C12 | Broker and agents not internet-reachable                                                       | `workers_dev: false`, no routes, service bindings only                                                                                              | wrangler config review                                |
| C13 | Secrets only in Secrets Store / Worker secrets                                                 | wrangler configs; `.gitignore`; gitleaks                                                                                                            | `security.yml`                                        |
| C14 | Supply chain: exact pins, release age, SHA-pinned actions                                      | `.npmrc`, `pnpm-workspace.yaml`, workflows                                                                                                          | `dependency-review`, `scorecard`                      |
| C15 | Prod deploy requires human approval                                                            | GitHub Environment `prod` required reviewers; hooks deny local deploys                                                                              | `.claude/hooks/block-dangerous.sh`                    |
| C16 | AI Gateway: BYOK, auth, logging, limits                                                        | `infra/terraform/modules/ai-gateway`, `AnthropicGateway` headers                                                                                    | manual                                                |
| C17 | Log hygiene: ids only, never bodies or tokens                                                  | `GitHubApiError`, structured `console.log`                                                                                                          | `api.test.ts`                                         |
| C18 | No secrets in files: credentials only via environment (CI secrets, macOS Keychain wrapper)     | `scripts/with-secrets.sh`; non-secret backend inlined in `infra/terraform/envs/*/main.tf`; hooks deny `.dev.vars`, `backend.hcl`, `*secret*.tfvars` | `.gitleaks.toml`; hooks exercised in every session    |

## Broker actions and the PAT permission each needs

| Action                                   | GitHub REST call                           | Fine-grained PAT permission                                |
| ---------------------------------------- | ------------------------------------------ | ---------------------------------------------------------- |
| `issue.comment`, `pr.comment`            | `POST /repos/{r}/issues/{n}/comments`      | Issues: read & write (PR comments use the issues endpoint) |
| `issue.label.add` / `issue.label.remove` | `POST/DELETE /repos/{r}/issues/{n}/labels` | Issues: read & write                                       |
| `pr.review`                              | `POST /repos/{r}/pulls/{n}/reviews`        | Pull requests: read & write                                |

Read-only inputs (listing issues, reading diffs) on public repositories need no token; when a token
is needed for rate limits, it will be a separate read-only PAT held by the broker (future `/v1/read`).
